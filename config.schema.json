{
	"pluginAlias": "Somfy Blinds",
	"pluginType": "accessory",
	"singular": false,
	"schema": {
		"type": "object",
		"properties": {
			"name": {
				"title": "Name",
				"description": "Name of the accessory as it will appear in Apple Home App",
				"type": "string",
				"default": "Blind 1",
				"required": true
			},
			"id": {
				"title": "ID",
				"description": "ID of the accessory. Must be unique.",
				"type": "integer",
				"default": 1000001,
				"required": true,
				"minimum": 0,
				"maximum": 16777216
			},
			"adminMode": {
				"title": "Admin Mode",
				"description": "Used to initially programme the remote",
				"type": "boolean",
				"default": true
			},
			"invertToggle": {
				"title": "Invert Direction",
				"description": "Used for blinds that go up",
				"type": "boolean",
				"default": false
			},
			"openToMyPosition": {
				"title": "Open to My Position",
				"description": "Sends MY button to open blinds in pre-set position",
				"type": "boolean",
				"default": false
			}
		}
	}
}